# Flask Blog

Многопользовательская блог-платформа с системой публикации постов и аутентификацией на основе дискриминаторов.

## Обзор

Flask Blog — это учебное приложение, демонстрирующее лучшие практики разработки на Flask:

- **Архитектура:** Application Factory с Service Layer Pattern
- **Аутентификация:** Discord-стиль с дискриминаторами (Username#1234)
- **База данных:** SQLite с чистым SQL (без ORM)
- **Безопасность:** CSRF-защита, хэширование паролей, безопасные cookie
- **Тестирование:** Комплексный набор тестов с покрытием >80%

## Технологический стек

- **Backend:** Python 3.13+, Flask 3.x
- **Database:** SQLite (чистый SQL)
- **Frontend:** Bootstrap 5, Jinja2
- **Testing:** Pytest
- **Security:** HMAC-SHA256 CSRF защита

## Быстрый старт

### Требования

- Python 3.13+
- pip

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd flask_blog

# Создание виртуального окружения
python -m venv .venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate  # Windows

# Установка зависимостей
pip install -r requirements.txt

# Инициализация базы данных
flask --app app init-db

# Запуск приложения
flask --app app run
```

Приложение будет доступно по адресу: http://127.0.0.1:5000

## Функциональность

### 🚀 Система авторизации

#### 💡 Основная концепция

Система авторизации построена вокруг **удобства пользователя** и вдохновлена Discord-стилем идентификации. Вместо традиционных email-логинов мы используем интуитивно понятные имена с дискриминаторами, что решает несколько ключевых проблем современных платформ.

#### 🎯 Проблемы, которые мы решаем

1. **Простота запоминания** - `User#1234` легче запомнить, чем `user.email+tag@gmail.com`
2. **Уникальность без компромиссов** - Множество пользователей могут иметь имя `Admin`, но с разными дискриминаторами
3. **Быстрый вход** - Поддержка автозаполнения браузеров и умные cookie
4. **Интуитивный интерфейс** - Минимальное количество полей для регистрации

#### 🏗️ Архитектурные решения

##### 1. Discord-стиль дискриминаторов

**Почему именно этот подход?**
- **Психологическая привычка** - Миллионы пользователей уже знакомы с этим форматом
- **Масштабируемость** - До 9999 пользователей с одинаковым именем (User#0001 - User#9999)
- **Читаемость** - Легко произнести и передать голосом

**Техническая реализация:**
```python
# Формат: Username#1234
# Admin - особый случай без дискриминатора
def format_full_username(username: str, discriminator: int) -> str:
    return f"{username}#{discriminator:04d}" if discriminator > 0 else username
```

##### 2. Гибридная система аутентификации

**Многоуровневая проверка логина:**
1. **Приоритет JavaScript** - Скрытое поле с полным логином имеет высший приоритет
2. **Cookie fallback** - Если JS недоступен, используем сохранённые cookie
3. **Базовый ввод** - Ввод полного логина вручную

**Почему такая сложность?**
- **Совместимость с браузерами** - Автозаполнение работает корректно
- **Graceful degradation** - Функциональность сохраняется без JavaScript
- **UX приоритет** - Пользователь видит простое поле, система делает сложную работу

##### 3. Умная система cookie

**localStorage-подобная функциональность через cookie:**
```python
# Сохраняем соответствие базовый_логин -> полный_логин
{
    "admin": "admin",
    "user": "user#1234",
    "john": "john#5678"
}
```

**Преимущества:**
- **Кросс-браузерность** - Работает везде, где есть cookie
- **Безопасность** - HttpOnly, Secure, SameSite=Strict
- **Множественные аккаунты** - Один браузер может помнить несколько пользователей

##### 4. Автоматическая генерация дискриминаторов

**Алгоритм генерации:**
1. Получаем все занятые дискриминаторы для имени
2. Вычисляем разницу множеств (свободные номера)
3. Выбираем случайный свободный номер
4. Если все заняты (9999 штук) - предлагаем другое имя

**Почему случайный выбор?**
- **Предсказуемость** - Нельзя заранее занять "красивые" номера
- **Равномерное распределение** - Избегаем скопления в начале диапазона
- **Безопасность** - Сложнее перебрать существующие аккаунты

#### 🔒 Безопасность

##### Многоуровневая защита паролей
```python
# PBKDF2-HMAC-SHA256 с 100k итераций
# Уникальная соль для каждого пользователя (16 байт)
# Формат хранения: pbkdf2_sha256$salt$hash
```

##### CSRF-защита
- HMAC-SHA256 подпись токенов
- Привязка к сессии пользователя
- Автоматическая валидация во всех формах

##### Безопасные cookie
- `HttpOnly=False` (для доступа из JavaScript)
- `Secure=True` (только HTTPS)
- `SameSite=Strict` (защита от CSRF)
- Срок жизни: 30 дней

#### 🎨 UX решения

##### Регистрация - 2 поля
- **Минимализм** - Только имя и пароль
- **Мгновенная обратная связь** - Дискриминатор генерируется сразу
- **Автовход** - После регистрации пользователь сразу в системе

##### Вход - умная форма
- **Автозаполнение** - Браузеры запоминают базовые логины
- **Кнопка обновления** - Один клик для добавления дискриминатора
- **Подсказки в реальном времени** - Динамическая помощь пользователю

##### Admin особый случай
- **Без дискриминатора** - `admin` вместо `admin#0000`
- **Автосоздание** - Если admin отсутствует в БД, создаётся автоматически
- **Конфигурационный пароль** - Задаётся через `config.py`

#### 🔄 Процесс аутентификации

##### Регистрация
```
1. Пользователь вводит имя и пароль
2. Система проверяет зарезервированные имена
3. Генерируется уникальный дискриминатор
4. Пароль хэшируется с солью
5. Создаётся запись в БД
6. Пользователь автоматически входит в систему
7. Сохраняется cookie для будущего автозаполнения
```

##### Вход
```
1. JavaScript проверяет сохранённые логины
2. При необходимости пользователь кликает "обновить"
3. Система проверяет логин в порядке приоритета:
   - Скрытое поле (JavaScript)
   - Cookie (localStorage-like)
   - Введённый текст
4. Для admin: специальная проверка
5. Для обычных пользователей: поиск по username#discriminator
6. Успешный вход → обновление cookie
```

#### 🛠️ Технические детали

##### Структура БД
```sql
CREATE TABLE user (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    username      TEXT    NOT NULL,
    password      TEXT    NOT NULL,  -- pbkdf2_sha256$salt$hash
    discriminator INTEGER NOT NULL, -- 0 для admin, 1-9999 для пользователей
    salt          BLOB    NOT NULL,  -- 16 байт
    UNIQUE (username, discriminator)
);
```

##### Ключевые компоненты
- **`app/auth/routes.py`** - Основная логика аутентификации
- **`app/auth/utils.py`** - Хэширование и генерация дискриминаторов
- **`app/static/js/login-discriminator-update.js`** - Управление формой входа
- **`app/static/js/auth-cookies.js`** - Cookie-менеджер

##### API endpoints
- `POST /auth/register` - Регистрация
- `POST /auth/login` - Вход
- `GET /auth/logout` - Выход
- `GET /auth/api/get-user-info` - Информация о текущем пользователе

#### 🎯 Преимущества подхода

1. **Удобство** - Минимальное количество действий для регистрации/входа
2. **Масштабируемость** - До 9999 пользователей с одним именем
3. **Совместимость** - Работает во всех браузерах, включая старые
4. **Безопасность** - Современные методы хэширования и защиты
5. **Гибкость** - Легко расширить для дополнительных фич (2FA, OAuth и т.д.)


### Управление постами

- **Создание:** Только авторизованные пользователи
- **Просмотр:** Доступно всем пользователям
- **Редактирование:** Только автор поста
- **Удаление:** Только автор поста
- **Отображение:** Список всех постов на главной странице

### Комментарии

- **Создание:** Только авторизованные пользователи
- **Просмотр:** Доступно всем пользователям
- **Удаление:** Только автор комментария
- **Отображение:** Интегрированы в страницу просмотра поста
- **Валидация:** Базовая проверка длины и обязательности полей

### Права доступа

- **Анонимные пользователи:** Просмотр постов и комментариев
- **Авторизованные пользователи:** Создание постов и комментариев
- **Авторы постов:** Полное управление своими постами
- **Авторы комментариев:** Удаление своих комментариев

## Архитектура

### Структура проекта

```
flask_blog/
├── app/
│   ├── auth/           # Аутентификация
│   ├── db/             # База данных
│   ├── forms/          # Валидация форм
│   ├── main/           # Основные страницы
│   ├── models/         # Модели данных
│   ├── services/       # Сервисный слой
│   ├── static/         # Статические файлы
│   └── templates/      # Шаблоны
├── tests/              # Тесты
├── docs/               # Документация
└── config.py           # Конфигурация
```

### Service Layer Pattern

- **Модели:** Данные и базовая бизнес-логика
- **Сервисы:** Вся логика работы с базой данных
- **Маршруты:** Обработка HTTP запросов
- Шаблоны: Отображение данных

## CI/CD с GitHub Actions

Проект использует автоматизированный пайплайн для обеспечения качества кода и стабильности:

### Что проверяется автоматически

- **🔍 Качество кода:** flake8 (стиль), black (форматирование), mypy (типы)
- **🔒 Безопасность:** bandit (сканирование уязвимостей)
- **🧪 Тестирование:** pytest с покрытием кода (>80%)
- **📊 Отчёты:** HTML отчёты покрытия и безопасности

### Преимущества

- **Нулевая ручная работа** - все проверки запускаются автоматически
- **Раннее обнаружение ошибок** - проблемы находятся до попадания в продакшн
- **Стабильность кода** - только качественный код попадает в main ветку
- **Визуальная обратная связь** - подробные отчёты о всех проверках
- **Артефакты** - можно скачать HTML отчёты покрытия и безопасности

### Как работает

1. **Push/Pull Request** → Автоматический запуск CI/CD
2. **Quality Checks** → Проверка стиля, форматирования, типов, безопасности
3. **Test Suite** → Запуск тестов с измерением покрытия
4. **Notification** → Уведомление о результате всех проверок

### Требования к коду

- Покрытие тестами не менее 80%
- Соответствие стилю кода (flake8)
- Правильное форматирование (black)
- Отсутствие уязвимостей безопасности (bandit)

## Разработка

### Тестирование

```bash
# Запуск всех тестов
pytest

# Тесты с покрытием
pytest --cov=app

# Быстрые тесты (без медленных)
pytest -m "not slow"

# Unit тесты
pytest -m "unit"
```

### Разработка без Makefile

В этом проекте не используется Makefile, так как все задачи решаются стандартными инструментами Python и IDE:

- **Запуск приложения:** `flask --app app run` или через IDE
- **Установка зависимостей:** `pip install -r requirements.txt`
- **Тестирование:** `pytest` напрямую
- **Линтинг:** встроенные линтеры IDE или `flake8 app`
- **Форматирование:** `black app` или через IDE
- **Очистка:** `find . -name "*.pyc" -delete && find . -name "__pycache__" -delete`

Такой подход упрощает разработку и не требует изучения дополнительных команд.

## Безопасность

### CSRF Защита

- HMAC-SHA256 подпись токенов
- Автоматическая валидация в формах
- Привязка к сессии пользователя

### Аутентификация

- PBKDF2-HMAC-SHA256 хэширование
- Уникальные соли для каждого пользователя
- HttpOnly cookie с флагами Secure и SameSite=Strict

## Статус проекта

### Реализовано
- ✅ Application Factory паттерн
- ✅ Система аутентификации с дискриминаторами
- ✅ CRUD для постов блога
- ✅ Полноценная система комментариев
- ✅ CSRF-защита
- ✅ Система тестов
- ✅ Service Layer архитектура

### В разработке
- 🔄 Валидация форм (базовые валидаторы - заглушки)
- 🔄 Пагинация
- 🔄 Мобильная адаптация

## Вклад в проект

Мы приветствуем вклад в развитие проекта!

1. Fork репозитория
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Требования

- Тесты должны проходить (`pytest`)
- Следуйте стилю кода проекта
- Добавьте тесты для новой функциональности

## Лицензия

MIT License

## Контакты

- [Telegram сообщество](https://t.me/python_nov)
- [Discord](https://discord.com/users/disciple_artem)