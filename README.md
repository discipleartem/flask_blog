# Flask Blog

Многопользовательская блог-платформа на Flask с возможностью публикации постов и обсуждений.

## Технологический стек

- **Backend:** Python 3.13, Flask 3.x
- **Database:** SQLite (чистый SQL без ORM)
- **Frontend:** Bootstrap 5, Jinja2
- **Testing:** Pytest

## Структура проекта

    flask_blog/
    ├── app/
    │   ├── auth/                   # Blueprint аутентификации
    │   │   ├── __init__.py
    │   │   ├── routes.py           # Маршруты (login, register, logout)
    │   │   └── utils.py            # Утилиты (хэширование, дискриминатор)
    │   ├── db/                     # Модуль базы данных
    │   │   ├── __init__.py
    │   │   ├── db.py               # Подключение и CLI-команды
    │   │   └── schema.sql          # SQL-схема таблиц
    │   ├── forms/                  # Валидация форм (без WTForms)
    │   │   ├── __init__.py         # Экспорт классов
    │   │   ├── base.py             # Базовые классы Form, Field
    │   │   ├── fields.py           # StringField, PasswordField, TextAreaField
    │   │   ├── validators.py       # Required, Length, Regexp, EqualTo
    │   │   └── csrf.py             # CSRF-защита
    │   ├── main/                   # Blueprint главной страницы
    │   │   ├── __init__.py
    │   │   └── routes.py
    │   ├── static/                 # CSS, JS, изображения
    │   ├── templates/
    │   │   ├── auth/
    │   │   │   ├── login.html
    │   │   │   └── register.html
    │   │   ├── base.html           # Базовый шаблон
    │   │   └── index.html          # Главная страница
    │   └── __init__.py             # Application Factory
    ├── docs/
    │   └── BlogPlatformDevelopmentPlan.md
    ├── instance/                   # SQLite база данных
    ├── tests/
    │   ├── conftest.py             # Pytest фикстуры
    │   ├── test_auth.py            # Тесты аутентификации
    │   └── test_forms.py           # Тесты валидации форм
    ├── .env                        # Переменные окружения (не в git)
    ├── .gitignore
    ├── config.py                   # Конфигурация приложения
    ├── pyproject.toml
    ├── README.md
    ├── requirements.txt
    └── wsgi.py                     # WSGI entrypoint

## Требования

- Python 3.13+

## Установка

1. **Клонируйте репозиторий:**

        git clone <repository-url>
        cd flask_blog

2. **Создайте виртуальное окружение:**

        python -m venv .venv

        # Linux/macOS:
        source .venv/bin/activate

        # Windows:
        .venv\Scripts\activate

3. **Установите зависимости:**

        pip install -r requirements.txt

   Для разработки (включая pytest):

        pip install -e ".[dev]"

## Инициализация базы данных

    flask --app app init-db

## Запуск

**Development:**

    flask --app app run --debug

**Production (Gunicorn):**

    gunicorn wsgi:app

Приложение доступно по адресу: http://127.0.0.1:5000

## Тестирование

    pytest

С покрытием:

    pytest --cov=app

### Команды для тестов

- `make test` - обычные тесты с покрытием
- `make test-clean` - чистые тесты без предупреждений

## Makefile

**Makefile** - это файл с набором команд для автоматизации часто выполняемых задач в проекте. Он позволяет использовать короткие и запоминающиеся команды вместо длинных консольных инструкций.

### Установка и управление зависимостями

- `make install` - установить основные зависимости проекта
- `make install-dev` - установить зависимости для разработки (включая pytest, black, flake8)

### Тестирование

- `make test` - запустить все тесты с детальной статистикой
  ```
  83 passed, 3 skipped, 4 warnings in 9.17s
  ```

- `make test-clean` - запустить тесты с чистым выводом (только результат)
  ```
  ✅ Все тесты пройдены!
  ```

- `make test-cov` - запустить тесты с покрытием кода и генерацией HTML-отчёта
- `make test-unit` - запустить только unit тесты
- `make test-integration` - запустить только интеграционные тесты
- `make test-all` - запустить все типы тестов

### Качество кода

- `make lint` - проверить код с помощью линтеров (flake8, mypy)
- `make format` - отформатировать код с помощью black
- `make clean` - очистить временные файлы и кэш

### Работа с приложением

- `make run` - запустить приложение в режиме разработки
- `make run-prod` - запустить приложение с gunicorn (продакшен-режим)
- `make shell` - запустить Flask shell для интерактивной работы

### База данных

- `make init-db` - инициализировать базу данных
- `make migrate` - выполнить миграции базы данных

### Разработка

- `make dev-setup` - настроить окружение для разработки (установка зависимостей + инициализация БД)
- `make check` - полная проверка проекта (lint + тесты)
- `make ci` - запустить CI pipeline (быстрые тесты + линтинг)

### Статистика и документация

- `make stats` - показать статистику проекта (количество строк кода)
- `make docs` - сгенерировать документацию

### Примеры использования

**Быстрый запуск тестов:**
```bash
make test-clean
```

**Настройка окружения для разработки:**
```bash
make dev-setup
```

**Проверка кода перед коммитом:**
```bash
make check
```

**Запуск приложения в разработке:**
```bash
make run
```

## Подавленные предупреждения

### Python 3.12+ Timestamp Converter Warning

**Описание:**
```
DeprecationWarning: The default timestamp converter is deprecated as of Python 3.12
```

**Почему подавлено:**
- Это только предупреждение, не ошибка
- Код продолжает работать корректно
- Старые стандарты работают стабильно
- Переход на новые стандарты требует значительных изменений

**Где подавлено:**

1. **Основной код** (`app/db/db.py`)
2. **Тесты** (`tests/conftest.py`)
3. **Конфигурация pytest** (`pytest-clean.ini`)

**Будущие изменения:**

Если потребуется перейти на новые стандарты Python 3.12+:
1. Убрать подавление предупреждений
2. Реализовать новые конвертеры временных меток
3. Обновить код работы с датами/временем

**Статус:** ✅ Подавлено - работаем по старым стандартам по требованию

## Особенности аутентификации

Система использует Discord-подобный подход:

- **Неуникальный логин** — можно выбрать любой логин, даже занятый
- **Дискриминатор** — автоматически генерируется тег #0001–#9999
- **Формат входа** — `Username#1234`
- **Безопасность** — PBKDF2-HMAC-SHA256 с уникальной солью

## Валидация форм

Проект использует собственную систему валидации без WTForms:

- **Базовые классы** — `Form` и `Field` для декларативного описания форм
- **Валидаторы** — `Required`, `Length`, `Regexp`, `EqualTo`
- **Типы полей** — `StringField`, `PasswordField`, `TextAreaField`
- **CSRF-защита** — токены на основе `session` и `SECRET_KEY`

## Текущий статус

**✅ Реализовано:**

- Application Factory паттерн
- Модульная структура с Blueprints (auth, main)
- Полная система аутентификации (регистрация, вход, выход)
- SQLite база данных с каскадными удалениями
- Собственная система валидации форм (без WTForms)
- Базовый UI на Bootstrap 5 с flash-сообщениями
- Защита маршрутов (login_required)
- Структура тестов с pytest

**🔄 В разработке:**

- CRUD для постов (Blueprint blog)
- Система комментариев
- Пагинация и мобильная адаптация

## Contributors

Мы приветствуем вклад в развитие проекта! Чтобы предложить изменения:

1. **Fork** репозитория.
2. **Создайте ветку** для своей фичи (`git checkout -b feature/AmazingFeature`).
3. **Закоммитьте** изменения (`git commit -m 'Add some AmazingFeature'`).
4. **Push** в ветку (`git push origin feature/AmazingFeature`).
5. Откройте **Pull Request**.

Пожалуйста, убедитесь, что ваши тесты проходят (`pytest`) и вы следуете стилю кода проекта.

## Контакты

Если у вас есть вопросы или предложения, вы можете связаться с нами:

- [Telegram сообщество](https://t.me/python_nov)
- [Discord](https://discord.com/users/disciple_artem)

## Лицензия

MIT