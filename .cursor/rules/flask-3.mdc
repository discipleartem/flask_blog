---
description: Flask 3.0.3 — factory, blueprints, стиль; карта — ARCHITECTURE
globs: app/**/*.py,wsgi.py,tests/**/*.py
alwaysApply: false
---

# Flask 3.0.3

Версия закреплена в `pyproject.toml` / `00-project.mdc`. Не поднимать без явного запроса.

Карта слоёв, blueprints и маршрутов: [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md). Scoping: [`AGENTS.md`](../../AGENTS.md).

## Алгоритм правки

1. Маршрут / view — только в соответствующем blueprint (`app/<bp>/`).
2. Новые маршруты или смена prefix — сразу правка таблицы «Маршруты» в ARCHITECTURE.
3. Секреты — только env / `.env`; не в коде.
4. Данные — SQL через `app/db.py` (`database.mdc`); auth — session (`auth.mdc`); UI — Jinja+BS5 (`ui-bootstrap.mdc`).

## Стиль

- Type hints на параметрах и возвратах; docstrings на русском
- Функции предпочтительнее классов (кроме нужды в состоянии); view = функция на blueprint
- Guard clauses / ранний `return`; happy path в конце
- Имена: `snake_case` файлы/функции; описательные флаги (`is_admin`, `has_permission`)
- Логирование: `current_app.logger` / `app.logger`, не `print` в прод-путях

## Запрос / ответ

- `before_request` / `teardown_appcontext` — как в factory и `init_db_app` (см. ARCHITECTURE §Поток)
- Flash + redirect для форм; не строить JWT/JSON-login API
- CSRF и session — существующие хелперы; не дублировать middleware

## Запрещено

- Flask-SQLAlchemy, Flask-Caching, connection pools «для Postgres»
- Gunicorn/uWSGI как обязательный локальный стек (прод — [`docs/DEPLOY.md`](../../docs/DEPLOY.md))
- Интеграционные/e2e-сюиты вместо unit (`testing.mdc`)

## Тесты

Flask test client внутри **`unittest`** (`tests/`); временная БД / `create_app` — как в существующих тестах.
