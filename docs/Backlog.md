# Backlog

Список будущих задач проекта. Подход к реализации выбирается при взятии задачи в работу.
Выполненные задачи фиксируются в [`CHANGELOG.md`](CHANGELOG.md) и из этого файла удаляются.

Синхронизация с [GitHub Project «Flask Blog»](https://github.com/users/discipleartem/projects/6): `python ~/.cursor/skills/backlog-github-projects-sync/scripts/sync_backlog.py` (конфиг [`.github/backlog.json`](../.github/backlog.json)).

Приоритеты доски (single select **Priority**): **`P0`** (критично) · **`P1`** (важно) · **`P2`** (низкий / продукт). Статусы: см. [GITHUB-PROJECTS-API.md](GITHUB-PROJECTS-API.md) и [backlog-status.mdc](../.cursor/rules/backlog-status.mdc).

Каждая задача — заголовок `## …` (одна GitHub Issue). Подзадачи — чеклисты внутри секции. Категория — поле `**Категория:**`.

---

## Fix: datetime — создание в Python и формат вывода

**Статус:** open
**GitHub:** #84
**Приоритет:** P2
**Категория:** Data / UI

### Проблема

Даты создаются в SQL через `datetime('now')` (формат `YYYY-MM-DD HH:MM:SS`) и так же показываются в шаблонах. Нужен единый Python-источник времени и человекочитаемый вывод `дд.мм.гггг чч:мм`.

### Acceptance criteria

- Создание / обновление `created_at` / `updated_at` задаётся из Python (UTC), а не через SQL `datetime('now')` в DEFAULT и в UPDATE.
- В UI даты отображаются как `дд.мм.гггг чч:мм` (например `01.08.2026 07:48`).
- Формат хранения в SQLite остаётся согласованным и sortable (UTC TEXT); шаблоны не парсят «вручную» разрозненно — общий хелпер/фильтр.
- Unit-тесты на хелпер формата (и при необходимости на запись timestamp из Python).

### Подзадачи

- [ ] Хелпер «сейчас UTC» + запись `created_at` / `updated_at` из Python (posts, comments, users, schema DEFAULT / миграция при необходимости)
- [ ] Jinja-фильтр или хелпер вывода `дд.мм.гггг чч:мм` во всех шаблонах с датами
- [ ] Unit-тесты
- [ ] Кратко обновить DEVELOPMENT / ARCHITECTURE при смене контракта дат

---

## Auth: rate limit / backoff на login

**Статус:** open
**GitHub:** #56
**Приоритет:** P1
**Категория:** Auth / Abuse prevention

### Проблема

`/auth/login` не ограничен — возможен brute-force по паролю.

### Acceptance criteria

- При серии неудачных попыток с одного IP (и/или login) включается задержка или временная блокировка.
- Успешный login сбрасывает счётчик для субъекта.
- Поведение задокументировано в DEVELOPMENT / ARCHITECTURE (кратко).

### Подзадачи

- [ ] Выбрать механизм (in-memory / SQLite bucket — без новой СУБД)
- [ ] Применить к login POST
- [ ] Unit-тесты на порог и сброс

---

## Auth: ужесточить политику пароля

**Статус:** open
**GitHub:** #58
**Приоритет:** P1
**Категория:** Auth / Hardening

### Проблема

Минимум 6 символов без требований к сложности / словарям.

### Acceptance criteria

- Задана явная политика (длина и/или классы символов) на register и смене пароля (если есть).
- Сообщения об ошибке не раскрывают лишнюю информацию о существующих пользователях.
- Unit-тесты на отказ слабых паролей.

### Подзадачи

- [ ] Правила валидации + константы/конфиг
- [ ] UI-подсказка в форме регистрации
- [ ] Тесты

---

## Auth (продукт): password reset / 2FA / роли

**Статус:** open
**GitHub:** #61
**Приоритет:** P2
**Категория:** Auth / Product

### Проблема

Нет сброса пароля, email-verify, 2FA; роль только `is_admin`. Для учебного блога ожидаемо; брать в работу только при реальной потребности.

### Acceptance criteria

- Перед реализацией — отдельное решение «нужно ли» (не делать «на будущее»).
- Если делается: минимальный вертикальный срез с тестами и docs.

### Подзадачи

- [ ] (Опционально) password reset flow
- [ ] (Опционально) 2FA
- [ ] (Опционально) роли шире admin/user — только с обоснованием

---

## UI: отображение пользователей (username + tooltip)

**Статус:** open
**GitHub:** #86
**Приоритет:** P2
**Категория:** UI / Users

### Проблема

В интерфейсе пользователи показываются с дискриминатором; нет подсказки с доп. информацией при наведении/нажатии на имя.

### Acceptance criteria

- Везде в UI (лента, пост, комментарии, списки) выводится только `username` **без** дискриминатора.
- При наведении (desktop) и нажатии/long-press (mobile) на имя — tooltip с полезной информацией (минимум: полный идентификатор / дискриминатор; при наличии — дата регистрации или статус).
- Поведение согласовано на всех страницах, где выводится автор.

### Подзадачи

- [ ] Единый хелпер/макрос Jinja для имени пользователя
- [ ] Tooltip (Bootstrap 5) на desktop и touch-friendly вариант
- [ ] Пройти шаблоны постов / комментариев / админ-списков (если уже есть)

---

## UI: главная — пагинация и счётчик статей

**Статус:** open
**GitHub:** #87
**Приоритет:** P2
**Категория:** UI / Posts

### Проблема

На главной нет пагинации; счётчик статей визуально слабый / некорректный.

### Acceptance criteria

- Пагинация ленты: **10** статей на страницу.
- Навигация по страницам (prev/next и/или номера), корректные query/path без дублей контента.
- Счётчик статей исправлен (отражает актуальное число) и визуально привлекателен в рамках текущей Bootstrap-темы.
- Unit-тесты на расчёт offset/limit и граничные страницы (пустая, последняя).

### Подзадачи

- [ ] Backend: limit/offset или keyset + total count
- [ ] UI пагинации на главной
- [ ] Редизайн/правка блока счётчика статей
- [ ] Unit-тесты

---

## Feat: админ-панель (таблицы users / posts / comments)

**Статус:** in_review
**GitHub:** #88
**Приоритет:** P1
**Категория:** Admin / UI

### Проблема

Нет единой админ-панели для просмотра и управления пользователями, статьями и комментариями.

### Acceptance criteria

- Доступ только для `is_admin` (session auth).
- Таблицы с **edit/delete**:
  - все пользователи;
  - все статьи;
  - комментарии с авторами — при переходе из списка статей (по выбранному посту).
- UI на Bootstrap 5 / Jinja, без SPA-фреймворков.
- (Опционально) Dashboard + интеграция с [PythonAnywhere](https://www.pythonanywhere.com/) (ссылки/статус деплоя — без усложнения ядра).
- Кратко в ARCHITECTURE / DEVELOPMENT.

### Зависимости

База для подключаемых модулей: загрузка изображений, локализация, подтверждение email.

### Подзадачи

- [x] Каркас `/admin/` (blueprint, layout, guard)
- [x] CRUD-таблица пользователей
- [x] CRUD-таблица статей
- [x] Комментарии по статье (drill-down из списка статей) с авторами
- [x] (Опционально) Dashboard + ссылки/статус PythonAnywhere
- [x] Unit-тесты прав доступа и базовых CRUD

---

## Feat: загрузка изображений (модуль админки)

**Статус:** open
**GitHub:** #89
**Приоритет:** P1
**Категория:** Admin / Media

### Проблема

Нет механизма загрузки и оптимизации изображений (посты, аватарки).

### Зависимости

**После** «Feat: админ-панель».

### Acceptance criteria

- Подключаемый модуль в админке (вкл/выкл или регистрация расширения без ломки ядра).
- Изменение размера и «веса» (сжатие) при загрузке.
- Оптимизация под mobile / tablet / desktop (responsive variants или srcset).
- Загрузка **аватарки** — только для пользователя с подтверждённым email.
- Загрузка **изображений в контент** — только для подтверждённого пользователя.
- Хранение на диске/static проекта (без новой объектной СУБД); docs по лимитам и путям.

### Подзадачи

- [ ] API/формы загрузки + валидация типов/размера
- [ ] Resize / compress pipeline
- [ ] Responsive variants (mobile/tablet/desktop)
- [ ] Gate: только verified user
- [ ] Точка подключения модуля в админке
- [ ] Unit-тесты валидации и отказа для unverified

---

## UI: аватарка для неподтверждённых пользователей

**Статус:** open
**GitHub:** #90
**Приоритет:** P2
**Категория:** UI / Users

### Проблема

У пользователей без подтверждённого email нет единообразного placeholder-аватара.

### Зависимости

**После** «Feat: загрузка изображений» (и логики verified из email-модуля).

### Acceptance criteria

- Для пользователей **без** подтверждённого email в UI показывается аватар на основе [Anonymous.svg](https://uk.wikipedia.org/wiki/%D0%A4%D0%B0%D0%B9%D0%BB:Anonymous.svg) (файл в `static/`, лицензия public domain — указать источник в docs/комментарии).
- Подтверждённые с загруженной аватаркой — своё изображение; без загрузки — согласованный fallback (не Anonymous, либо тот же — зафиксировать в AC при реализации).

### Подзадачи

- [ ] Добавить SVG в static (локальная копия)
- [ ] Хелпер URL аватара (verified + path / Anonymous)
- [ ] Подключить в шаблонах (комментарии, профиль, списки)

---

## Feat: локализация (модуль админки)

**Статус:** open
**GitHub:** #91
**Приоритет:** P2
**Категория:** Admin / i18n

### Проблема

Интерфейс только на одном языке; нет расширения локалей из админки.

### Зависимости

**После** «Feat: админ-панель».

### Acceptance criteria

- Подключаемый модуль в админке.
- По умолчанию локали **RU** и **EN**.
- Через админку можно добавить новую локаль (минимум: код + отображаемое имя; строки — файл/таблица по выбранному подходу при реализации).
- (Опционально) определение страны по IP → соответствующая локаль.
- (Опционально) Google Translate для стран/языков без своей локали.
- Docs: как добавить строки и локаль.

### Подзадачи

- [ ] Каркас i18n (Flask-Babel или лёгкий свой слой — решить при взятии в работу)
- [ ] Модуль в админке: список локалей + добавление
- [ ] Дефолт RU/EN + переключатель языка
- [ ] (Опционально) GeoIP → locale
- [ ] (Опционально) Google Translate fallback
- [ ] Unit-тесты выбора локали

---

## Feat: подтверждение аккаунта по email (модуль админки)

**Статус:** open
**GitHub:** #92
**Приоритет:** P1
**Категория:** Auth / Admin

### Проблема

Нет email-verify; связанные фичи (аватар, бейдж, загрузки) требуют статус «подтверждён».

### Зависимости

**После** «Feat: админ-панель». Связано с продуктовым пунктом email-verify в задаче «Auth (продукт): password reset / 2FA / роли» — при реализации закрывать/сужать тот пункт.

### Acceptance criteria

- Подключаемый модуль в админке (провайдер, секреты через env, не в git).
- По умолчанию отправка через **Gmail** (Google account **app password** / app-почта), не «личный» произвольный SMTP без явной настройки.
- Токен подтверждения, срок жизни, повторная отправка; UI «письмо отправлено» / «подтверждено».
- (Опционально) провайдеры: [Resend](https://resend.com/), [Amazon SES](https://aws.amazon.com/ses/), [Brevo](https://www.brevo.com/).
- Поле/флаг verified в БД + миграция; unit-тесты потока.

### Подзадачи

- [ ] Схема: `email_verified` / token table + миграция
- [ ] Провайдер Gmail (app password) по умолчанию
- [ ] Flow: register → send → confirm link
- [ ] Настройки модуля в админке
- [ ] (Опционально) Resend / SES / Brevo adapters
- [ ] Unit-тесты

---

## UI: значок подтверждённого пользователя

**Статус:** open
**GitHub:** #93
**Приоритет:** P2
**Категория:** UI / Users

### Проблема

Подтверждённый аккаунт визуально не отличается от неподтверждённого.

### Зависимости

**После** «Feat: подтверждение аккаунта по email».

### Acceptance criteria

- Рядом с именем (и/или аватаром) отображается значок для `email_verified`.
- Загрузка аватарки по-прежнему только для подтверждённого пользователя (согласовано с модулем изображений).
- Доступность: `aria-label` / title «Подтверждённый пользователь».

### Подзадачи

- [ ] Иконка/бейдж в общем макросе пользователя
- [ ] Согласовать с tooltip и аватаром
- [ ] Проверка в шаблонах ленты / поста / комментариев
