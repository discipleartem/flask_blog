# План разработки Блог-платформы на Flask

Этот документ содержит пошаговый план создания многопользовательской блог-платформы с использованием Flask, SQLite (чистый SQL) и Bootstrap 5.

## Этап 1: Инициализация проекта и скелет приложения

- [x] Создание структуры папок (`app/`, `static/`, `templates/`, `tests/`, `docs/`).
- [x] Настройка базового конфигурационного файла `config.py`.
- [x] Создание фабрики приложений в `app/__init__.py`.
- [x] Настройка базового шаблона `base.html` с подключением Bootstrap 5.
- [x] Настройка `pyproject.toml` и `requirements.txt`.
- [x] Создание `wsgi.py` для production-развёртывания.

## Этап 2: Работа с базой данных (SQLite)

- [x] Создание файла `app/db/schema.sql` (таблицы: user, post, comment).
- [x] Настройка модуля подключения к БД (`app/db/`).
- [x] CLI-команда для инициализации БД (`flask init-db`).

## Этап 3: Аутентификация и пользователи

**Подход:** Discord-стиль — логин не уникален, каждому пользователю присваивается уникальный дискриминатор (тег),
например `Username#1234`. Регистрация только по логину и паролю, без email и подтверждений.

- [x] Обновление схемы БД: добавлено поле `discriminator` в таблицу `user`.
- [x] Регистрация Blueprint `auth` (`app/auth/`).
- [x] Функционал регистрации:
  - Любой логин (даже если занят).
  - Автоматическая генерация уникального дискриминатора (#0001–#9999).
  - Хэширование паролей (PBKDF2-HMAC-SHA256 с солью, без внешних библиотек).
- [x] Функционал логина (ввод в формате `Логин#1234`).
- [x] Функционал логаута.
- [x] Middleware для защиты маршрутов (`login_required` декоратор).
- [x] Шаблоны: `auth/register.html`, `auth/login.html`.
- [x] Обновление `base.html`: навигация, flash-сообщения.
- [x] Базовые тесты аутентификации (`tests/test_auth.py`).

## Этап 3.1: Валидация форм (без WTForms)

**Подход:** Собственная система валидации без внешних зависимостей.

**Текущий статус:** Архитектура готова, валидаторы - заглушки

- [x] Создание модуля валидации (`app/forms/`):
  - [x] Базовый класс `Form` с методами `validate()` и `errors`.
  - [x] Базовый класс `Field` (StringField, PasswordField, TextAreaField).
  - [x] Набор валидаторов-заглушек: `Required`, `Length`, `Regexp`, `EqualTo`.
- [x] CSRF-защита:
  - [x] Генерация токена на основе `session` и `SECRET_KEY`.
  - [x] Валидация токена при отправке формы.
  - [x] Хелпер `csrf_token()` для шаблонов.
- [x] Интеграция с шаблонами:
  - [x] Макросы для рендеринга полей и ошибок.
- [x] Рефакторинг форм аутентификации на новую систему.
- [x] Тесты валидации (`tests/test_forms.py`, `tests/test_validation_stubs.py`).

**TODO для будущей реализации:**
- [ ] Реализация `DataRequired` - проверка обязательных полей
- [ ] Реализация `Length` - проверка минимальной/максимальной длины
- [ ] Реализация `Regexp` - самописная регулярная валидация
- [ ] Реализация `EqualTo` - проверка совпадения полей
- [ ] Валидаторы: `Email`, `Username`, `PasswordStrength`, `UniqueUsername`
- [ ] Валидаторы: `Slug`, `NumberRange`, `URL`

## Этап 4: Управление статьями (CRUD)

- [x] Регистрация Blueprint `main` для главной страницы (`app/main/`).
- [x] Главная страница с приветствием пользователя (`index.html`).
- [x] Список всех постов на главной странице.
- [x] Создание нового поста (форма, сохранение).
- [x] Просмотр отдельного поста.
- [x] Редактирование поста (только для автора).
- [x] Удаление поста (только для автора).
- [x] Модель поста (`app/models/post.py`).
- [x] Сервисы для работы с постами.

## Этап 5: Комментарии

- [x] Таблица `comment` в схеме БД (author_id, post_id, content, created).
- [x] Модель комментария (`app/models/comment.py`).
- [x] Сервис для работы с комментариями (`app/services/comment_service.py`).
- [x] Форма добавления комментария (`app/forms/comment.py`).
- [x] Маршруты для добавления/удаления комментариев.
- [x] Полностью интегрированная система комментариев в `view_post.html`.
- [x] Отображение списка комментариев с информацией об авторах.
- [x] Удаление своих комментариев.

## Этап 6: Стилизация и UI/UX

- [x] Базовая стилизация через Bootstrap 5.
- [x] Навигационная панель (динамические ссылки: Войти/Выйти).
- [x] Flash-сообщения об успехах/ошибках.
- [ ] Пагинация для списка постов.
- [ ] Адаптивный дизайн для мобильных устройств.

## Этап 7: Финализация и Документация

- [x] Полное покрытие тестами (auth, forms, database, integration, main, security, posts, discriminator).
- [x] Настройка CI/CD pipeline через GitHub Actions.
- [x] Обновление `README.md` полной инструкцией.
- [x] Service Layer Pattern с моделями и сервисами.
- [x] Makefile для автоматизации разработки.
- [ ] Финальное тестирование.
- [ ] Документация API/маршрутов.

---

## Прогресс

| Этап                | Статус        |
|---------------------|---------------|
| 1. Инициализация    | ✅ Завершён    |
| 2. База данных      | ✅ Завершён    |
| 3. Аутентификация   | ✅ Завершён    |
| 3.1. Валидация форм | 🔄 В процессе |
| 4. Статьи (CRUD)    | ✅ Завершён    |
| 5. Комментарии      | ✅ Завершён    |
| 6. UI/UX            | 🔄 В процессе |
| 7. Финализация      | 🔄 В процессе |

## Достигнутые вехи

**✅ Выполнено:**
- Полноценная система аутентификации с дискриминаторами в стиле Discord
- Service Layer Pattern с чётким разделением моделей и сервисов
- Собственная система форм с заглушками валидаторов
- CSRF-защита на основе сессий
- Комплексная система тестов (9 типов тестов: auth, forms, database, integration, main, security, posts, discriminator, validation_stubs)
- Автоматизация разработки через Makefile
- CI/CD pipeline с GitHub Actions
- Чистый SQL без ORM
- Application Factory паттерн
- Полноценный CRUD для постов блога
- Система авторов и прав доступа
- Модели и сервисы для работы с постами
- Фильтр шаблонов `nl2br` для форматирования текста
- **Полноценная система комментариев** с моделями, сервисами, формами и маршрутами
- Интеграция комментариев в шаблоны с отображением авторов и возможностью удаления

**🔄 В разработке:**
- Реализация валидации форм (архитектура готова, валидаторы - заглушки)
- Пагинация и мобильная адаптация

**📋 Очередные задачи:**
- Реализация базовых валидаторов (DataRequired, Length, EqualTo)
- Добавление пагинации для списка постов
- Мобильная адаптация интерфейса
- Финальное тестирование и документация API